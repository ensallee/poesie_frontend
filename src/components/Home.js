import React, { Fragment } from 'react';

const Home = () => {
  return (
    <Fragment>
    <section class="hero">
      <div class="hero-inner">
        <h1>Poesie</h1>
        <a href="https://example.com/" class="btn">Explore</a>
        </div>
    </section>
    </Fragment>
  )
}

export default Home;
